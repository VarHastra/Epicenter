<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.varhastra.epicenter">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentation.placenamepicker.PlaceNamePickerActivity"></activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".presentation.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.settings.SettingsActivity"
            android:label="@string/app_settings" />
        <activity
            android:name=".presentation.details.DetailsActivity"
            android:label=""
            android:theme="@style/AppTheme" />
        <activity
            android:name=".presentation.placesmanager.PlacesManagerActivity"
            android:label="@string/places_title" />
        <activity
            android:name=".presentation.placeeditor.PlaceEditorActivity"
            android:theme="@style/AppTheme.TransparentStatus"
            android:label=""/>
    </application>

</manifest>